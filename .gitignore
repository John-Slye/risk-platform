# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# uv / virtual envs
.venv/
venv/
env/

# IDE / OS
.vscode/
.idea/
.DS_Store

# Data (gitignored — never commit datasets to a public repo)
data/raw/
data/processed/
data/*.csv
data/*.parquet
*.zip

# Model artifacts (heavy binaries — retrain locally)
models/*.pkl
models/*.joblib
models/*.cbm
# `models/metrics.json` IS tracked: small file, bundles validation metrics
# for the Model Cards dashboard page.

# Reports / large outputs
report.pdf
*.html
notebooks/*-executed.ipynb

# Build / distribution
build/
dist/

# Docker
*.dockerignore.tmp
INTERVIEW_PREP.md
